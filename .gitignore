# ROS 2 Build Artifacts
build/
install/
log/
.colcon/

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.venv/
env/

# IDE
.vscode/
.idea/

# Antigravity User Specific
# We WANT to track .agent, but maybe not some internal state if any
# For this repo, we track everything in .agent

# User Code (Don't track actual ROS 2 packages created by user, except tools)
src/*
!src/tools/
!src/tools/setup_linter.py
!src/tools/context_provider.py

# GeneratedConfigFiles (Track these as templates or let setup script generate them? 
# Let's ignore them so they are generated fresh by setup script, preventing conflict)
setup.cfg
.clang-format

# System
.DS_Store
